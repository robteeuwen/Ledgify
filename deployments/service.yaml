apiVersion: v1
kind: Service
metadata:
  name: ledgify-service
spec:
  ports:
  - port: 80 # this port is accessible to the outside world
    targetPort: 8000 # this is the container port that it's linked to
    name: http
  selector:
    app: ledgify